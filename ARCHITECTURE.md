# 🚨 JobShield AI - Architecture Overview

## 🏗️ System Architecture

```
                         ┌─────────────────────┐
                         │   User Browser      │
                         │  (http://localhost  │
                         │      :5173)         │
                         └──────────┬──────────┘
                                    │
                    ┌───────────────│───────────────┐
                    │                               │
              ┌─────▼────────┐            ┌────────▼──────┐
              │   Frontend    │            │   API Calls   │
              │  React/Vite   │────────────│   (Axios)     │
              │               │            │               │
              │ ┌────────────┐ │            └────────┬──────┘
              │ │Pages       │ │                      │
              │ │- Landing   │ │                      │
              │ │- Form      │ │                      │
              │ │- Results   │ │                      │
              │ │- History   │ │                      │
              │ └────────────┘ │                      │
              └────────────────┘                      │
                    (Port 5173)                       │
                                                      │
                    ┌─────────────────────────────────▼───────────────┐
                    │  Node.js Backend (Port 5000)                    │
                    │  ┌──────────────────────────────────────────┐   │
                    │  │Express.js Server                        │   │
                    │  ├──────────────────────────────────────────┤   │
                    │  │ API Endpoints                            │   │
                    │  │ POST   /api/analyze                      │   │
                    │  │ GET    /api/history                      │   │
                    │  │ GET    /api/analysis/:id                 │   │
                    │  │ GET    /api/stats                        │   │
                    │  │ GET    /api/health                       │   │
                    │  └──────────────────────────────────────────┘   │
                    │                                                  │
                    │  ┌──────────────┬────────────┐                  │
                    │  │              │            │                  │
                    │  │ Controllers  │ Services   │                  │
                    │  │              │            │                  │
                    │  │ └─────────┐  │ ┌─────────┐│                  │
                    │  │ │Analysis │  │ │Analysis ││                  │
                    │  │ │Handler  │  │ │Service  ││                  │
                    │  │ └─────────┘  │ ├─────────┤│                  │
                    │  │              │ │Rule-    ││                  │
                    │  │              │ │Based    ││                  │
                    │  │              │ │Detector ││                  │
                    │  │              │ ├─────────┤│                  │
                    │  │              │ │AI Client││                  │
                    │  │              │ │         ││                  │
                    │  │              │ └─────────┘│                  │
                    │  └──────────────┴────────────┘                  │
                    └─────────┬──────────────────────┬─────────────────┘
                              │                      │
                    ┌─────────▼──────────┐  ┌────────▼──────────┐
                    │  MongoDB Atlas     │  │  Python AI        │
                    │  (Database)        │  │  Service          │
                    │  Port 27017        │  │  (Port 5001)      │
                    │  (Cloud)           │  │                   │
                    │                    │  │ Flask App         │
                    │ ┌────────────────┐ │  │ ┌────────────────┐│
                    │ │JobAnalysis     │ │  │ │ML Model        ││
                    │ │Collection      │ │  │ │├────────────────┤│
                    │ │- _id           │ │  │ ││Logistic Reg.   ││
                    │ │- Risk Score    │ │  │ ││TF-IDF Vector   ││
                    │ │- Red Flags     │ │  │ ││Scikit-learn    ││
                    │ │- Timestamps    │ │  │ │└────────────────┘│
                    │ └────────────────┘ │  │                   │
                    │                    │  │ Endpoints:        │
                    └────────────────────┘  │ POST /predict     │
                                            │ GET /health       │
                                            │ GET /stats        │
                                            └───────────────────┘
```

---

## 🔄 Data Flow

```
USER INPUT (Browser)
    ↓
┌─────────────────────────┐
│ Frontend (React)        │
│ - Validate form         │
│ - Show loading state    │
└────────┬────────────────┘
         │ (Axios POST)
         ↓
┌─────────────────────────┐
│ Backend (Express)       │
│ - Validate inputs       │
│ - Create job object     │
└────────┬────────────────┘
         │
         ├─────────────────────────┬────────────────────────┐
         │                         │                        │
         ↓                         ↓                        ↓
    ┌────────────────┐      ┌────────────────┐      ┌────────────────┐
    │ Rule-Based     │      │ AI Predict     │      │ Save to DB     │
    │ Engine         │      │ (Python)       │      │ (MongoDB)      │
    │                │      │                │      │                │
    │ 8+ Rules       │      │ ML Model       │      │ Store all      │
    │ Score: 0-100   │      │ Probability    │      │ analysis data  │
    │                │      │ Score: 0-1     │      │                │
    └────────┬───────┘      └────────┬───────┘      └────────┬───────┘
             │                       │                       │
             └───────────┬───────────┴───────────┬───────────┘
                         │                       │
                         ↓                       │
         ┌───────────────────────────────────┐   │
         │ Combine Scores (50% each)         │   │
         │ Final Score = 0-100               │   │
         │ Risk Level = Low/Medium/High      │   │
         └───────────┬───────────────────────┘   │
                     │                           │
                     ├───────────────────────────┘
                     ↓
         ┌───────────────────────┐
         │ Generate Results      │
         │ - Risk Score          │
         │ - Red Flags           │
         │ - Suggestions         │
         │ - Explanations        │
         └───────────┬───────────┘
                     │ (JSON Response)
                     ↓
         ┌───────────────────────┐
         │ Frontend (React)      │
         │ - Display results     │
         │ - Show visualizations │
         │ - Enable actions      │
         └───────────┬───────────┘
                     │
                     ↓
           ┌─────────────────────┐
           │ USER SEES RESULTS   │
           │ Risk Score: 85      │
           │ Risk Level: HIGH    │
           │ Red Flags: 5        │
           │ Safety Tips: 8      │
           └─────────────────────┘
```

---

## 📊 Risk Calculation Formula

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│  Rule-Based Score (0-100)                             │
│  ├─ Email domain check          (+0-20)               │
│  ├─ Payment keywords            (+0-40)               │
│  ├─ Phone validity              (+0-15)               │
│  ├─ Website check               (+0-10)               │
│  ├─ Salary realism              (+0-25)               │
│  ├─ Urgency tactics             (+0-15)               │
│  └─ Grammar errors              (+0-10)               │
│                   ↓                                     │
│          [Sum, cap at 100]                             │
│                   ↓                                     │
│  Total Rule-Based: RB_Score ──┐                        │
│                                 │                       │
│  AI Model Score                  │                       │
│  ├─ TF-IDF vectorization        │                       │
│  ├─ Logistic Regression         │                       │
│  └─ Probability (0-1)   ──────┐ │                       │
│                                 │ AI_Score × 100        │
│                                 │                       │
│  Final Calculation:              │                       │
│  ┌───────────────────────────────┼───────────────────┐   │
│  │                               │                   │   │
│  │  Final_Score = (RB × 0.5) + (AI × 0.5 × 100)    │   │
│  │                                                   │   │
│  │  Result: 0-100                                   │   │
│  │  ├─ 0-39:   Low Risk    🟢 (Legitimate)         │   │
│  │  ├─ 40-59:  Medium Risk 🟡 (Verify)             │   │
│  │  └─ 60-100: High Risk   🔴 (Avoid)              │   │
│  └───────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🗂️ Module Dependencies

```
Frontend (React)
│
├── Components
│   ├── Header
│   ├── Pages (Landing, Form, Results, History)
│   └── Styles (6 CSS files)
│
├── Utils
│   └── API Client (Axios)
│       └── Connects to Backend
│
└── Dependencies
    ├── react@18
    ├── vite
    └── axios

Backend (Node.js)
│
├── Server (Express)
│   ├── Routes
│   │   └── Analysis Routes
│   │
│   ├── Controllers
│   │   └── Analysis Controller
│   │
│   ├── Services
│   │   ├── Analysis Service
│   │   ├── Rule-Based Detector
│   │   └── AI Client
│   │
│   ├── Models
│   │   └── JobAnalysis (Mongoose)
│   │       └── Connects to MongoDB
│   │
│   ├── Middleware
│   │   └── Error Handlers, CORS, Logging
│   │
│   └── Utils
│       └── Validation
│
└── Dependencies
    ├── express
    ├── mongoose
    ├── cors
    ├── express-validator
    └── axios (calls AI)

AI Service (Python)
│
├── Flask App
│   ├── Routes
│   │   ├── /health
│   │   ├── /predict
│   │   └── /stats
│   │
│   └── ML Model
│       ├── Training Data
│       ├── Vectorizer (TF-IDF)
│       ├── Classifier (Logistic Regression)
│       └── Model Persistence
│
└── Dependencies
    ├── Flask
    ├── Scikit-learn
    ├── Numpy/Pandas
    └── Spacy

Database (MongoDB)
│
└── JobAnalysis Collection
    ├── Indexes
    └── Automatic timestamps
```

---

## 🔌 API Connection Points

```
Frontend                          Backend                      Database
   │                               │                             │
   │  POST /api/analyze            │                             │
   ├──────────────────────────────>│                             │
   │                               │ Validate Input              │
   │                               │ ├─ Format Check             │
   │                               │ ├─ Length Check             │
   │                               │ └─ Type Check               │
   │                               │                             │
   │                               │ Call Services               │
   │                               │ ├─ Rule-Based Detection     │
   │                               │ ├─ AI Prediction (Python)   │
   │                               │ └─ Score Combination        │
   │                               │                             │
   │                               │ Save Results                │
   │                               ├────────────────────────────>│
   │                               │                             │
   │  Results                       │<────────────────────────────┤
   │<──────────────────────────────┤ (Saved Analysis ID)         │
   │                               │                             │
   │  GET /api/history             │                             │
   ├──────────────────────────────>│                             │
   │                               │ Query Database              │
   │                               ├────────────────────────────>│
   │                               │                             │
   │  Analysis List                │<────────────────────────────┤
   │<──────────────────────────────┤ (All Previous Records)      │
   │                               │                             │
```

---

## 🔐 Security Flow

```
Request from Frontend
    ↓
┌──────────────────────────┐
│ CORS Check               │  ✓ Origin allowed?
│ (middleware)             │
└────────┬─────────────────┘
         │
         ↓
┌──────────────────────────┐
│ Input Validation         │  ✓ Format correct?
│ (express-validator)      │  ✓ Length valid?
└────────┬─────────────────┘  ✓ Type correct?
         │
         │ ❌ Error → Return 400 Bad Request
         │
         ↓
┌──────────────────────────┐
│ Input Sanitization       │  ✓ Remove XSS?
│ (utils/validation)       │  ✓ Strip tags?
└────────┬─────────────────┘
         │
         │ ❌ Error → Return 400 Bad Request
         │
         ↓
┌──────────────────────────┐
│ Process Request          │  ✓ Proceed safely
│ (services)               │
└────────┬─────────────────┘
         │
         ↓
┌──────────────────────────┐
│ Error Handling           │  ✓ No stack traces
│ (middleware)             │  ✓ Generic messages
└────────┬─────────────────┘
         │
         ↓
Response to Frontend
(Safe, validated data)
```

---

## 📈 Scalability Architecture

```
Current (Single Server)
┌─────────────────────────────────────────┐
│ All Services on Localhost               │
│ ├─ Frontend: 5173                       │
│ ├─ Backend: 5000                        │
│ ├─ AI: 5001                             │
│ └─ MongoDB: localhost                   │
└─────────────────────────────────────────┘

Scaled (Production)
┌─────────────────────────────────────────┐
│ Vercel (Frontend)                       │
│ ├─ Auto-scaling                         │
│ ├─ CDN distribution                     │
│ └─ 99.9% uptime SLA                     │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ Render/Railway (Backend)                │
│ ├─ Auto-scaling                         │
│ ├─ Load balancing                       │
│ ├─ Health monitoring                    │
│ └─ Automatic restarts                   │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ Render/Railway (AI Service)             │
│ ├─ GPU support (optional)               │
│ ├─ Auto-scaling                         │
│ ├─ Model caching                        │
│ └─ Batch processing ready               │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ MongoDB Atlas (Database)                │
│ ├─ Cloud hosted                         │
│ ├─ Automatic backups                    │
│ ├─ Sharding capable                     │
│ └─ 99.99% uptime SLA                    │
└─────────────────────────────────────────┘
```

---

## 🚀 Deployment Pipeline

```
Local Development
    ↓
GitHub Repository
    ↓
    ├─────────┬──────────┬──────────┐
    │         │          │          │
    ↓         ↓          ↓          ↓
  Vercel   Render    Render     Atlas
Frontend  Backend     AI         DB
    ↓         ↓          ↓          ↓
┌─────────────────────────────────────┐
│ All Services Live & Connected       │
│ http://myapp.vercel.app             │
└─────────────────────────────────────┘
    ↓
Production Monitoring
    ├─ Error tracking
    ├─ Performance metrics
    ├─ User analytics
    └─ Automated alerts
```

---

This comprehensive architecture ensures:

✅ **Scalability** - Handle thousands of users  
✅ **Reliability** - 99.9% uptime  
✅ **Security** - Multiple validation layers  
✅ **Performance** - Fast response times  
✅ **Maintainability** - Clear separation of concerns  
✅ **Extensibility** - Easy to add features  

---

**Ready to deploy to production!** 🚀
