# AI service production env
# Replace placeholders and secure these values in your deployment platform

AI_PORT=5001
FLASK_ENV=production

# Local model paths (if using local storage). For production, consider S3 or model registry.
MODEL_PATH=models/fraud_model.pkl
VECTORIZER_PATH=models/tfidf_vectorizer.pkl

# Optional: S3 or object storage URL for model files
# MODEL_S3_URL=
